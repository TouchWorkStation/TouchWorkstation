#!/usr/bin/env bash
set -e
[ "$EUID" -eq 0 ] || { echo 'Run with sudo.'; exit 1; }
source /etc/touchworkstation/touchworkstation.env
echo "URL: http://$(hostname).local:8088"
echo "Password: $APP_PASSWORD"
